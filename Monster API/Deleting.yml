info:
  name: Deleting
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/monsters/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
